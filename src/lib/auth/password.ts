/** Single source of truth for password length when creating or changing passwords. */
export const MIN_PASSWORD_LENGTH = 8;

export const PASSWORD_MIN_ERROR = "Password must be at least 8 characters.";
